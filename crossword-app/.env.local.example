# .env.local
# Copy this file as .env.local and fill in your values.

# PartyKit dev server (default — don't change for local dev)
NEXT_PUBLIC_PARTYKIT_HOST=127.0.0.1:1999

# After deploying to PartyKit cloud, replace with your deployment URL:
# NEXT_PUBLIC_PARTYKIT_HOST=crossword-app.YOUR_USERNAME.partykit.dev
